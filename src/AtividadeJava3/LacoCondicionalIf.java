package AtividadeJava3;

import java.util.Scanner;

public class LacoCondicionalIf {

	public static void main(String[] args) {
		int a, b, c, num;
		String palavra1, palavra2, palavra3;

		Scanner entrada = new Scanner(System.in);

		// 1 - Faça um algoritmo em Java que leia 3 valores inteiros A, B e C e imprima
		// na tela se a soma de A + B é maior, menor ou igual a C.
		/*
		 * System.out.println("\nDigite o número A: "); a = entrada.nextInt();
		 * System.out.println("\nDigite o número B: "); b = entrada.nextInt();
		 * System.out.println("\nDigite o número C: "); c = entrada.nextInt();
		 * 
		 * if (a + b > c) { System.out.println("\nA soma de " + a + " + " + b +
		 * " é MAIOR do que " + c); } else if (a + b < c) {
		 * System.out.println("\nA soma de " + a + " + " + b + " é MENOR do que " + c);
		 * } else { System.out.println("\nA soma de " + a + " + " + b + " é IGUAL a " +
		 * c); }
		 */

		// 2- Escreva um algoritmo em Java, que leia um número inteiro via teclado e
		// mostre na tela uma mensagem indicando se este número é par ou ímpar e se o número é
		// positivo ou negativo.
		
		/*
		 System.out.println("\nDigite um número: "); num = entrada.nextInt(); 
		 if (num % 2 == 0 && num >= 0) { System.out.println("\nO número " + num + " é par e positivo.");
		  	} else if (num % 2 == 0 && num < 0) {
		 		System.out.println("\nO número " + num + " é par e negativo.");
		 	} else if(num != 2 && num > 0) { 
		 		System.out.println("\nO número " + num + " é ímpar e positivo."); 
		 	} else { 
		 		System.out.println("\nO número " + num +" é ímpar e negativo."); }
		 */
		
		//2- Resolução do instrutor
		
		
		

		// 4 - Escreva um algoritmo em Java, que leia 3 palavras (String), que definem as
		// características de um tipo de animal possível segundo o diagrama abaixo, que
		// deve ser lido da esquerda para a direita (feito com o instrutor)
		/*
		System.out.println("\nEntre com a primeira palavra: ");
		palavra1 = entrada.next();
		System.out.println("\nEntre com a segunda palavra: ");
		palavra2 = entrada.next();
		System.out.println("\nEntre com a terceira palavra: ");
		palavra3 = entrada.next();
		
		if(palavra1.equalsIgnoreCase("vertebrado")) {
			if(palavra2.equalsIgnoreCase("ave)) {"
			
		}
		*/
	}
}
